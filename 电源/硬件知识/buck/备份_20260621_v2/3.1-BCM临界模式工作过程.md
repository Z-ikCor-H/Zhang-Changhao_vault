# 第三章 Buck DCM原理 第一节：BCM工作过程

本章开始讲解Buck电路的**DCM（Discontinuous Conduction Mode，断续导通模式）**原理。作为过渡，本节先讨论**BCM（Boundary Conduction Mode，临界导通模式）**——即CCM与DCM的分界线。

---

## 一、从CCM到BCM：两种进入路径

假设已按CCM模式设计好一个Buck变换器（L、D、f_s等参数固定）。在实际工作中，有两种情况会使变换器进入临界模式。

![[3.1-1.png|473]]
### 1.1 路径一：负载减轻（$R_0 \uparrow$）

在满载时，变换器工作于CCM模式，电感电流波形为连续三角波。

当负载功率下降，即负载电阻 $R_0$ 增大：
- 输出电压 $U_o$ 不变（闭环调节）
- 输出电流 $I_o = U_o / R_0$ **下降**
- 电感电流平均值 $I_L = I_o$ **下降**

但电感电流纹波 $\Delta i_L$ 几乎不变：
$$\Delta i_L = \frac{U_o}{L} \cdot (1-D) \cdot T$$

> **原因：** 在CCM模式下，$U_o$ 不变、$L$ 不变，占空比 $D = U_o/U_{in}$ 与负载几乎无关，因此 $\Delta i_L$ 基本保持不变。

因此，电感电流波形发生的是**向下平移**——平均值下降，但纹波幅度不变。

![[3.1-2.png|439]]

随着 $R_0$ 继续增大，电感电流不断下移，最终**谷底刚好触碰到零点**：

$$I_{L\_min} = I_o - \frac{\Delta i_L}{2} = 0$$

此时变换器进入**BCM模式**。对应的负载电阻称为**临界电阻** $R_{crit}$。

> 若 $R_0$ 继续增大（负载继续减轻），电感电流将出现"断流"，进入DCM模式。

---

### 1.2 路径二：电感减小（$L \downarrow$）

保持满载（$R_0$ 和 $I_o$ 不变），但减小电感值 $L$。

由纹波公式：
$$\Delta i_L = \frac{U_o}{L} \cdot (1-D) \cdot T$$

$L \downarrow$ → $\Delta i_L \uparrow$（纹波增大）

但电感电流平均值 $I_L = I_o$ 保持不变（负载未变）。

![[3.1-3.png|408]]

- 电感电流的**中点**（平均值）固定不变
- 纹波幅度逐渐增大——峰值升高、谷底降低
- 最终谷底刚好触碰到零点：$I_{L\_min} = I_o - \Delta i_L/2 = 0$

此时对应的电感值称为**临界电感** $L_{crit}$。

> 若 $L$ 继续减小（$L < L_{crit}$），变换器将在全负载范围内进入DCM模式。

---

## 二、临界电阻与临界电感

### 2.1 BCM模式下的电流关系

在临界点，电感电流谷底为零，波形为**从零开始、以零结束**的三角波：

![[3.1-4.png|380]]

此时：
- $I_{L\_min} = 0$
- $I_{L\_peak} = I_{LP} = \Delta i_L$
- 平均值 $I_o = I_L = \frac{1}{2} I_{LP} = \frac{1}{2} \Delta i_L$

由此得到BCM模式的核心特性：

$$\boxed{I_o = \frac{1}{2} I_{LP} = \frac{1}{2} \Delta i_L}$$

### 2.2 临界电阻 $R_{crit}$

将 $\Delta i_L = \frac{U_o}{L}(1-D)T$ 代入 $I_o = \frac{1}{2}\Delta i_L$：

$$I_o = \frac{1}{2} \cdot \frac{U_o}{L}(1-D)T$$

又 $R_0 = U_o / I_o$，代入得：

$$\boxed{R_{crit} = \frac{2L}{(1-D) \cdot T}}$$

- 当 $R_0 > R_{crit}$（负载更轻）→ 进入DCM
- 当 $R_0 = R_{crit}$ → BCM临界模式
- 当 $R_0 < R_{crit}$（负载更重）→ CCM模式

### 2.3 临界电感 $L_{crit}$

将 $\Delta i_L = \frac{U_o}{L}(1-D)T$ 代入整理：

$$\boxed{L_{crit} = \frac{R_0 \cdot (1-D) \cdot T}{2}}$$

- 当 $L < L_{crit}$ → 全范围进入DCM
- 当 $L = L_{crit}$ → BCM临界模式
- 当 $L > L_{crit}$ → 在设计负载范围内可工作于CCM

---

## 三、BCM模式的关键特性：$r = 2$

电流纹波率定义为：
$$r = \frac{\Delta i_L}{I_o}$$

在BCM模式下：
$$\Delta i_L = I_{LP}, \quad I_o = \frac{1}{2}I_{LP}$$

因此：

$$\boxed{r = \frac{\Delta i_L}{I_o} = 2}$$

> **这是一个普适结论，不限于Buck拓扑。** 对于所有基本拓扑（Buck、Boost、Buck-Boost），当 $r = 2$ 时，电感均进入临界导通模式（BCM）。

| 模式 | $r$ 范围 | 电感电流特征 |
|---|---|---|
| CCM | $r < 2$ | 谷底 > 0，始终连续 |
| BCM | $r = 2$ | 谷底 = 0，刚好触零 |
| DCM | $r > 2$ | 出现零电流平台期 |

---

## 四、BCM与CCM/DCM的关系

BCM是CCM与DCM的**无缝衔接点**：
- BCM既可以看作CCM的极限情况（$I_{L\_min} \to 0$）
- 也可以看作DCM的极限情况（$T_D \to 0$，断续时间为零）
- CCM和DCM的公式在BCM点上**统一且连续**

这一特性在后续推导DCM增益公式时会有体现——将BCM条件代入DCM公式，自然退化为CCM的 $U_o/U_{in} = D$。

---

## 五、本节小结

| 主题 | 核心结论 |
|---|---|
| 进入BCM的两种方式 | ① 负载减轻（$R_0 \uparrow$）→ 电流下移；② 电感减小（$L \downarrow$）→ 纹波增大 |
| 临界电阻 | $R_{crit} = \dfrac{2L}{(1-D)T}$，$R_0 > R_{crit}$ 则进入DCM |
| 临界电感 | $L_{crit} = \dfrac{R_0(1-D)T}{2}$，$L < L_{crit}$ 则全范围DCM |
| BCM特性 | $I_o = I_{LP}/2$，$r = 2$（适用于所有基本拓扑） |
| 衔接特性 | BCM是CCM与DCM的无缝过渡点 |

---

## 六、与后续内容的衔接

- 下一节：当 $R_0 > R_{crit}$ 或 $L < L_{crit}$，进入**DCM工作过程**（出现第三模态 $T_D$）
- 第三节：DCM模式下**增益表达式**的推导
- 第四节：DCM模式下**各元件电流应力**的计算
